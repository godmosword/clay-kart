# BAR-CONTENT — 內容完整度

> 第五份 bar。R36 新增。
>
> **其餘四份 bar 全部在問「已經存在的東西夠不夠好」。這一份問「東西存在嗎」。**

---

## §0 為什麼需要這份

36 輪、11 天之後，`BAR-FEEL` 46/46、`BAR-PERF` 15/17，而遊戲的實際狀態是：

```
賽道        1 條環形
道具系統    0
音效        0 個檔
角色        6 個寫在 CHARACTERS.md，實作 1 個
視覺元件    6 / 12
結算畫面    無
選單        無
```

**沒有任何一條既有檢查會因為這些而變紅。** 四份 bar 可以全綠，
而那個東西不是一個遊戲。

這是本專案抓到的第十一種「檢查不可能失敗」，也是最貴的一種——
前十種讓某個指標失去鑑別力，這一種讓**整個開發方向**失去回饋。

### §0.1 這份 bar 的性質跟其他四份不同

| | 其他四份 | 這一份 |
|---|---|---|
| 問的問題 | 這東西夠不夠好 | 這東西在不在 |
| 判準 | 數值落在窗口內 | **布林值，true 或 false** |
| 可否調整 | 窗口可經裁決重訂 | **不得調整。** 只能做出來，或誠實記 FAIL |

**沒有窗口就沒有「調鬆一點讓它過」這個選項。** 那是刻意的——
這份 bar 存在的唯一理由，就是它必須擋得住我們自己。

---

## §1 檢查方式

全部機械檢查，零 LLM。`tools/validate/content.py`，跟 `feel.py`／`perf.py` 同一個形狀。

**每一條的答案只有 true／false。** 沒有窗口、沒有 `not_applicable`、
沒有 `_finite()` 正規化——那三樣正是 `§4.1`／`§2.5`／`§5.2` 曾經失效的方式。

未實作就是 `false` 就是 FAIL。**FAIL 不是壞事，是這份 bar 正常運作的樣子。**

---

## §2 最小可玩集（W4 完成判準）

| # | 條款 | 怎麼機械判定 |
|---|---|---|
| 2.1 | **比賽會結束並顯示結果** | 跑完 `totalLaps` 之後，DOM 出現含名次與總時間的結算元素 |
| 2.2 | **結算畫面可以重新開始** | 結算元素含一個可點擊的重新開始控制項，點下去後 `lap` 回到 1 |
| 2.3 | **有音效** | build 產物含至少 1 個音訊檔，且遊戲執行中 `AudioContext` 至少播放過一次 |
| 2.4 | **引擎聲隨速度變化** | 兩個不同速度取樣點的音訊參數（音高或音量）不相同 |
| 2.5 | **有道具箱** | 場上存在可拾取物件，且碰到之後玩家狀態改變 |
| 2.6 | **道具種類 ≥ 2** | 固定 seed 下重複拾取，出現至少兩種不同效果 |
| 2.7 | **賽道 ≥ 2 條** | 存在兩組不同的賽道幾何定義，且可切換 |
| 2.8 | **車手 ≥ 3 位可選** | `CHARACTERS.md` 定義 6 位；至少 3 位有實作且可被選擇 |

### §2.9 這份清單刻意不含的東西

- **不含畫面好不好看**——那是 `BAR-VISUAL`
- **不含手感**——那是 `BAR-FEEL`
- **不含效能**——那是 `BAR-PERF`
- **不含「道具平衡」「賽道有趣」**——那是設計品質，這份只問存不存在

混進品質判斷會讓這份 bar 變得可以爭辯，而它的價值就在於**不可爭辯**。

---

## §3 選單與流程（W5，先寫下來但不擋 W4）

| # | 條款 |
|---|---|
| 3.1 | 有起始畫面，不是一載入就直接開跑 |
| 3.2 | 可以選車手 |
| 3.3 | 可以選賽道 |
| 3.4 | 有暫停 |

---

## §4 讀這份 bar 的方式

**全綠不代表遊戲好玩。** 它只代表「該有的東西都在」。

好不好玩要靠真人試玩——R34 的真人試玩五分鐘找到的東西
（撞牆卡死、手感不流暢、貼圖不夠精緻），比 36 輪的自動化檢查加起來都多。
那不是自動化的失敗，是**自動化本來就測不到那些**。

所以流程上：`BAR-CONTENT` 全綠 → 真人試玩 → 回報 → 才談品質 loop。
