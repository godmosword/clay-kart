# BAR-PERF — 效能標準

> 第三個 bar。視覺 / 手感 / **效能**,三者獨立驗收。
>
> 這份 bar 的 critic 是**確定性程式**,不是 LLM —— 讀 profiler 輸出與 frame log 比對窗口。
> 成本為零,可以每輪跑。

---

## §0 為什麼要獨立成一個 bar

`BAR-FEEL` 驗的是模擬,`BAR-VISUAL` 驗的是靜態截圖。兩者都可以在一台 M 系列 Mac 上全 PASS,
而在 iPad 上跑 22fps —— 目標受眾(3–7 歲)用的正是 iPad 和中階手機。

**效能不是最佳化議題,是驗收議題。** 放到最後才處理,等於 W3 的黏土材質全部要重做。

---

## §1 目標裝置

| 檔位 | 代表機種 | 目標 |
|---|---|---|
| 基準 | iPad(第 9 代)/ 中階 Android | **60 fps 穩定** |
| 下限 | iPhone SE 世代 | ≥ 45 fps,不得低於 30 |
| 開發機 | M 系列 Mac | 不作為驗收依據 |

**在開發機上量測的數字一律不採計。** 這是本 bar 最容易被繞過的地方。

### §1.1 目前沒有任何通往基準機的量測路徑（R23 認定）

到 R23 為止產生過的每一份 perf artifact 都來自**不採計的環境**：

| 輪次 | 環境 | `fps_p50` | 採計? |
|---|---|---:|---|
| R16 | 開發機（方塊車） | 59.88 | 否 |
| R20 | 容器，SwiftShader 軟體算繪、無 GPU | 6.6 | 否 |
| R22 | 開發機 M4（黏土） | 34.84 | 否 |
| R23 | 開發機 M4 / 容器 | 22.37 / 8.32 | 否 |

也就是說 `§2` 從來沒有被真正判決過。過去幾輪圍繞 `2.1`–`2.5` 的
FAIL 所做的討論，**判的都是不構成驗收依據的數字**。這不是誰的疏漏，
是這條路徑一直不存在而沒有人明講。

**規則（即刻生效）：**

1. 非基準機的量測**照跑照記**——它們是有用的相對訊號（R20 用同機器比出
   「VSM 陰影佔 12 倍」、R23 用同機器背對背比出「舊 GC 量測重複計數 25 倍」，
   兩個結論都成立且都不需要基準機）
2. 但這類 artifact 的 `meta.environment` 必須標明，且**其 `VERDICT` 不得
   被引用為 `§2`／`§5` 的驗收結論**。只能說「這台機器上的相對變化」
3. **絕對值跨機器、跨負載一律不可比。** R23 實測同一份 build 在
   load ~5.7 下 `fps_p50` 是 22，稍早較閒時是 34.8——同樣的程式碼差 1.6 倍。
   任何沒有附上環境與當時負載的 fps 數字都不構成證據
4. `§2` 的驗收在基準機量測路徑建立之前**維持未判決**，不是 PASS 也不是 FAIL

**這是目前 `BAR-PERF` 最大的缺口**，而且它比任何單項指標都前置：在 iPad
或中階 Android 上跑得起來、量得到 frame log 之前，`§1` 的整張表都只是意向。

---

## §2 幀率

| ID | 指標 | 窗口 | 說明 |
|---|---|---|---|
| 2.1 | `fps_p50` | `[58, 62]` | 基準機,單圈全程 |
| 2.2 | `fps_p05` | `[55, 62]` | 第 5 百分位,抓卡頓 |
| 2.3 | `frame_time_p99_ms` | `[0, 22]` | 單幀最壞情況 |
| 2.4 | `long_frame_count` | `[0, 3]` | 單圈內 > 33ms 的幀數 |
| 2.5 | `gc_pause_max_ms` | `[0, 8]` | 主迴圈不得配置垃圾 |

**2.5 的意義:** 每幀 new 物件會讓 GC 在隨機時刻插入停頓,
這會同時破壞 `BAR-FEEL §2` 的觀感一致性。物理與渲染主迴圈一律用預先配置的緩衝。

---

## §3 載入

| ID | 指標 | 窗口 | 說明 |
|---|---|---|---|
| 3.1 | `first_interactive_s` | `[0, 3.0]` | 首屏可操作,4G 網速 |
| 3.2 | `initial_bundle_kb_gz` | `[0, 900]` | gzip 後,含 three |
| 3.3 | `total_assets_mb` | `[0, 12]` | 首場賽事所需全部素材 |
| 3.4 | `time_to_first_render_s` | `[0, 1.5]` | 看到畫面(可以是 loading 場景) |

> three.js 本身 gzip 後約 128 kB(W1 骨架實測),3.2 的預算大部分留給材質與 mesh。

---

## §4 抽格規則驗證(來自 `CHARACTERS.md §3`)

視覺 critic 在 contact sheet 上看不出抽格,所以由程式檢查:

| ID | 指標 | 窗口 | 說明 |
|---|---|---|---|
| 4.1 | `character_anim_hz` | `[11.5, 12.5]` | 角色動畫實測更新率 |
| 4.2 | `vehicle_transform_hz` | `[58, 62]` | 載具 transform,**不得抽格** |
| 4.3 | `camera_hz` | `[58, 62]` | 相機,**不得抽格** |

**4.2 或 4.3 落到 12 附近 = 抽格套錯對象,整輪 FAIL。**
這是最容易做錯的一條:builder 想做定格感,順手把整個 scene 都抽格了,遊戲就毀了。

### §4.4 這道防線目前是虛設的（R23 認定，窗口不變）

`perf-probe.mjs` 把 `vehicle_transform_hz` 與 `camera_hz` **都設成同一個
`renderedHz`**（`rendered_frames / elapsed`）。它量的是「算繪出了幾幀」，
不是「載具 transform 更新了幾次」「相機更新了幾次」。

後果是這條防線**測不到它被寫下來要防的那件事**：一個以 12Hz 更新 transform
但以 60Hz 算繪的場景，`4.2`／`4.3` 會回報 60，照樣 PASS。而那正是
「builder 順手把整個 scene 都抽格」的樣子。

同時 `4.2` 與 `4.3` 現在是彼此的別名，不是兩個獨立訊號——只要 fps 不達標
它們就必然一起 FAIL，而 `§6` 把 `§4` 排在優先序第一，於是 `largest_gap`
會指向「抽格套錯對象」，實際問題卻是「算繪很慢」。R23 的 verdict 就是這樣
指錯地方的。

**窗口與整輪 FAIL 的規則都不變**——問題在量測不在標準，調窗口只會把
壞掉的儀器合法化。要修的是儀器：需要在 render path 裡對 transform 與
camera 的**實際更新事件**計數，而不是數算繪幀。那落在 `src/render/`
（ck-visual 範圍），不是探針單方面做得到的。

**在儀器修好之前，`4.2`／`4.3` 的 PASS 不構成證據**，它們的 FAIL 也應該
讀成「算繪慢」而不是「抽格套錯對象」。這一條與 `§1.1` 是同一個性質：
數字有在跑，但它沒有在量它宣稱要量的東西。

---

## §5 記憶體與資源

| ID | 指標 | 窗口 | 說明 |
|---|---|---|---|
| 5.1 | `heap_peak_mb` | `[0, 320]` | 基準機 |
| 5.2 | `heap_growth_per_lap_mb` | `[0, 2]` | 洩漏偵測,連跑五圈 |
| 5.3 | `draw_calls` | `[0, 150]` | 單幀 |
| 5.4 | `triangles_k` | `[0, 400]` | 單幀,千個三角形 |
| 5.5 | `texture_memory_mb` | `[0, 96]` | |

**5.2 是唯一必須跑五圈才測得出來的指標。** 其餘單圈即可。

---

## §6 優先序(critic 選 largest_gap 的依據)

1. **§4 全部** — 抽格套錯對象會同時毀掉手感與定格感
2. **2.1 / 2.2** — 幀率是體驗底線
3. **5.2** — 記憶體洩漏會讓長時間遊玩崩潰
4. **2.3 / 2.4 / 2.5** — 卡頓
5. **§3 全部** — 載入
6. **5.1 / 5.3 / 5.4 / 5.5** — 資源用量

同層級多項 FAIL 取相對偏離最大者:`abs_deviation / window_width`。

---

## §7 量測方式

由 Codex 實作於 `tools/telemetry/perf-probe`(與 `ghost-replay` 同一波):

- 在 `ghost-replay` 的固定輸入序列上量測,確保跑分可比
- 輸出 `loop/round-{N}/artifacts/perf-{device}.json`
- `tools/validate/perf.py` 讀該 JSON 對本文件窗口判 PASS/FAIL,輸出 `VERDICT.json`
- **這支腳本不得呼叫任何 LLM API**

**基準機量測需要真實裝置。** 在 CI 沒有 iPad 之前,可暫用 Chrome DevTools 的
4x CPU throttling 當代理指標,但 `verdict` 必須標註 `"device": "proxy"`,
且 proxy 的 PASS 不得寫進 `FROZEN.md`。

---

## §8 何時跑

- **W1 結束:** 只跑 §2.1、§3.4。骨架階段其餘不適用
- **W2 每輪:** 跑 §2、§5.2。手感調校最容易意外拖垮幀率
- **W3 每輪:** 全部。黏土材質是效能主要風險
- **W4:** 全部
