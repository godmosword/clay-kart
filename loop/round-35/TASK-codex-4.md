# R35 第四件（ck-physics）—— `§4` 的 tier probe 要在無牆環境重測

你上一件的逐 tick 軌跡**證偽了我的假設，而且證偽的方式比假設成立更有價值**。
我猜「以前擦牆、現在不擦」，實際是兩版都擦牆，變的是擦牆的軌跡。
你的結論「5.873 與 3.595 都不是乾淨的漂移量測」我採納。

**但你的軌跡還露出一件你沒有特別點出的事**，那才是這一輪的重點：

```
tier2 量測窗內接觸幀：204（tick 398–678）
```

**`§4.5` 也是沿著牆量的。** 它的數字前後逐位元相同
（`1.5603197783466292`），但**不變不等於有效**——它只是剛好沒被這次的
邊界重解析擾動到。

而 `§4.5` 是 `BAR-FEEL §9` 指定的**唯一硬門檻**。
從 R5 到 R34，`§4` 的 tier 收益**從來沒有在無牆環境下被量過**。

---

## 裁決（已寫進 `loop/BACKLOG.md`，這裡複述範圍）

1. **窗口全部不動**：`§4.5` `[1.5, 2.5]`、`§4.7` `[2.8, 4.0]`、
   `§6.3` `[0.20, 0.45]`。**在拿到乾淨量測之前改窗口，等於把污染合理化。**
2. **不改漂移物理**。目前沒有任何證據說漂移本身有問題——
   有問題的是量它的地方。
3. `§6.3` 這一輪仍然不動（它也是從牆碰撞量的，同一個污染問題）。

## 要求

### 一、tier probe 移到無牆環境

tier1／tier2／tier3 的量測窗內，**每一幀的 `wall_contact` 都必須是 false**。

做法你決定（加大賽道半徑的 probe 專用 fixture、換起始位置與航向、
拉長離牆距離都可以），但有兩條硬性：

- **不得為了避開牆而縮短量測窗**——那會改變 tier 收益的定義
- **probe 若做不到零接觸，要誠實 FAIL**，回報
  `wall_contaminated_measurement` 之類的明確狀態，
  **不要照樣輸出一個數字**。跟 `§5.2` 的 `incomplete_five_lap_run`
  是同一個形狀。

### 二、把「量測窗內的接觸幀數」變成 artifact 的常設欄位

三個 tier 各自回報 `contact_frames_in_window`。
**這樣下次有人動碰撞，污染會立刻看得出來，而不是三十輪後才翻出來。**

### 三、拿到乾淨數值後，如實回報，不要調整任何東西

我預期三個數字都會變。**它們很可能落在現有窗口之外——那是正常的**，
因為現有窗口是照污染過的數字調出來的。

**回報數字就好，窗口重訂是我的裁決。**

## 交件

- 三個 tier 的乾淨收益值，各附 `contact_frames_in_window = 0` 的證據
- 完整 `feel.py` 46 項，逐項對照 R34／R35
- artifact commit 進 `loop/round-35/artifacts/`
- push 到 `feat/physics`，回報 commit sha

## FROZEN

`tools/telemetry/ghost-replay.mjs` **沒有凍結**，probe 就在那支，隨你改。
`feel.py` 的 `WINDOWS` 不動。`world.ts` 這一輪唯讀。

## 之後還有一件

`TASK-codex-2.md`（轉向響應量測）仍然開著，排在這件之後。
使用者已指認「手感不流暢」的來源是轉向響應，那件是純量測、`world.ts` 唯讀。
