# R35 第三件（ck-physics）—— `§4.7` 為什麼只有 tier3 受影響

撞牆卡死那件收了，我獨立重跑 `ghost-replay` + `feel.py`，跟你逐項一致：
44/46、`§4.10` 十位數不變。**probe 證明卡死解除**（只按倒車 1 幀後速度
0.03437、只按轉向 1 幀後 yaw delta 0.00522）。

你誠實回報兩項 FAIL 而且**沒有去調窗口讓它變綠**——任務明文禁止那件事，
你遵守了。這在這個專案裡不是理所當然（R14 發生過用配置值頂替實測）。

---

## 這一輪只查一件事：`§4.7` 的根因

```
§4.7  car_lengths_gained_tier3   3.595 → 5.873   窗口 [2.8, 4.0]   超出上限 47%
```

**而同一輪 `§4.5`（tier2）與 `§4.10`（retention）完全沒動**，
`§4.10` 連小數第十位都一樣。

**只有 tier3 動了 47%，這件事本身需要解釋。** 一個碰撞逃脫的修正
不該選擇性地只影響一個 tier。

### 我的假設（要驗，不是要你照著做）

tier3 的 probe 路徑原本**會擦到牆而被減速**，現在不黏牆所以跑得更遠。
若是這樣，變的是**量測環境**不是漂移本身——`§4.7` 應該回到 3.6 附近，
而現在的 5.873 才是「沒有被牆干擾」的真值。

### 要求

1. **拿出 tier3 probe 的逐 tick 軌跡**，包含每幀的
   `wall_contact`、到邊界的距離、地面速度。
   **直接回答：那段路徑在 R35 之前有沒有碰到邊界？**
2. 同一份資料給 tier2 做對照——若 tier2 的路徑完全不靠近牆，
   「只有 tier3 受影響」就得到解釋。
3. 若假設不成立（tier3 從來沒碰過牆），**那就是真的回歸**，
   要查為什麼碰撞改動會經由別的路徑影響漂移獲益。回報你找到什麼，
   不要猜。

### 明文禁止

- **不得調整 `§4.7` 的窗口 `[2.8, 4.0]`**，也不得調 `§6.3` 的 `[0.20, 0.45]`。
  窗口要不要改是 Lead 的裁決，而且我要等這條查清楚才會一起下——
  兩條都動 `BAR-FEEL` 的窗口，分開下會前後矛盾。
- **不得為了讓 `§4.7` 回到窗口內去改漂移參數**。在根因不明之前調參數，
  等於用一個未知去補另一個未知。
- `§6.3` 這一輪完全不要動，我已經傾向裁決「下限不再適用於修好之後的碰撞」，
  但要等 `§4.7`。

### 交件

- tier3 與 tier2 的逐 tick 軌跡資料，commit 進 `loop/round-35/artifacts/`
- 一句話結論：**probe 環境變了** 還是 **漂移真的變強了**
- 若是前者，附上「把牆的干擾排除後，R34 的 tier3 真值是多少」
- push 到 `feat/physics`，回報 commit sha

## 順帶

`TASK-codex-2.md`（轉向響應量測）還開著，排在這件之後。
那件是純量測、`world.ts` 唯讀，不會跟這件衝突。
