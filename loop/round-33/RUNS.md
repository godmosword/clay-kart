# R33 三輪 critic —— 執行紀錄

> **主產出不是分數，是三輪之間的變異。** 見 `loop/BACKLOG.md` 的 R33 裁決。
>
> R24 量到的 ±2 分變異是在一張壞掉的表上量的（標籤洩漏＋256 色量化＋
> 構圖不對稱）。四層修完之後變異掉了沒有，是這一輪要回答的事。
> 分數是次要的。

## 送審材料（三輪必須完全相同）

| 檔案 | sha256 | 用途 |
|---|---|---|
| `artifacts/contact-sheet.png` | `292efc2b08c7876f831693d118e18afdc1257f4532f648b73e271676b7c39e3f` | `§1` A/B 給分 |
| `artifacts/game-scene.png` | `6cc2f61a7e9a0df4f135b5a2b483c539010bd3b3f96c2c8f167ea17cab316ee8` | `§1.4`，不給分 |

提示詞：`CRITIC-PROMPT.md`（三輪逐字相同，不要改）

## 協定

1. **三輪各自獨立 session。** 不要在同一個 session 裡跑第二輪。
2. **每輪跑完立刻**把回覆原文存成 `artifacts/CRITIC-run{N}.md` 並 commit，
   **再開下一輪**。R32 是整輪跑完才寫，中斷即全損。
3. 每輪開頭要求 critic 先報 `§1.2` 索引校驗（雙佔位格的位置）。
   **校驗錯的那一輪，分數不可採計**，但仍然算一次變異樣本。

## 進度

- [ ] run 1 → `artifacts/CRITIC-run1.md`
- [ ] run 2 → `artifacts/CRITIC-run2.md`
- [ ] run 3 → `artifacts/CRITIC-run3.md`
- [ ] 彙整 → `VERDICT-visual.json` + `CRITIC-NOTES.md`

## 讀法（R33 裁決時先講死，數字出來後不得更改）

有效 A/B 配對只有 **4 組**。判讀規則：

- **≥ 2 組跨輪差距 ≥ 2 分** → 儀器仍解析不到門檻。分數**不構成閘**，
  這是 R34 的題目，不是元件的題目。
- **4 組全部差距 < 2** → 中位數可用，`§1.1` 是一道真的閘。
- 兩種情況下，`§0.1` 的警告都要跟分數一起讀：黃金樣本不在對比表上，
  **越照標準做的輸出越可能被判為與參考不同**。

`§1.1` 規則 3：**偏好方向比絕對分數穩定**，兩者不一致時以偏好為準。

## Lead 側的校驗答案

**不要貼進 critic 的 session。** 留在這裡是為了收件時能立刻判斷索引有沒有錯位。
配對本身在 `artifacts/contact-sheet.key.json`（已 gitignore，不進倉庫）。

雙佔位校驗組共 4 格。critic 回報的集合與實際不符即為索引錯位。
